import { proBackSchema } from '../../../models/profile'

interface Props {
  data: proBackSchema[]
}

function Profile() {
  return (
    <div>
      <h1> im the profile </h1>
    </div>
  )
}

export default Profile
